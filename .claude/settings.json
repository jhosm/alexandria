{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$TOOL_INPUT_file_path\" in *.env|*.env.*|*.db|*package-lock.json) echo 'BLOCKED: refusing to edit sensitive or generated file' && exit 1;; esac"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier --write \"$TOOL_INPUT_file_path\" 2>/dev/null || true"
          },
          {
            "type": "command",
            "command": "case \"$TOOL_INPUT_file_path\" in *.ts) npx eslint --fix \"$TOOL_INPUT_file_path\" 2>/dev/null || true;; esac"
          }
        ]
      }
    ]
  }
}
